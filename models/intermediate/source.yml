version: 2

models:
  - name: int_local_bike_orders_complete
    description: "staff and store informations for each orders"
    columns:
    - name: order_id
      description: "Unique identifier for order"
      tests:
        - not_null

  - name: int_local_bike_products_complete
    description: "full informations for each products"
    columns:
    - name: product_id
      description: "product informations for each orders"
      tests:
        - not_null
