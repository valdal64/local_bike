version: 2

models:
  - name: brands
    description: "Information about the product brands."
    columns:
      - name: brand_id
        description: "Unique identifier for the brand."
        tests:
          - unique
          - not_null
      - name: brand_name
        description: "Name of the brand."
        tests:
          - not_null